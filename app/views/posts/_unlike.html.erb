<%= form_with(model: current_user.likes.find_by(post_id: @post.id), url: post_likes_url(@post), html: { method: :delete}, local: true) do |f| %>
    <%= f.submit "いいねを取り消す", class: "btn btn-width btn-danger" %>
<% end %>